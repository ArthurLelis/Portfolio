// import { useActiveSection } from '@/hooks/useActiveSection';

export function Footer() {
  // const { setActiveSection, setTimeOfLastClick } = useActiveSection();

  return (
    <footer className="!pt-30 sm:!pt-40">

    </footer>
  );
}
